query GetOrderById($customerAccessToken: String!) {
    customer(customerAccessToken: $customerAccessToken) {
        id
        orders(first: 250, reverse: true) {
            nodes {
                ...OrderCoreFields
                lineItems(first: 100) {
                    nodes {
                        ...OrderLineItemFields
                    }
                }
            }
        }
    }
}