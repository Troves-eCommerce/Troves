query GetOrderById($customerAccessToken: String!, $language: LanguageCode!) @inContext(language: $language) {
    customer(customerAccessToken: $customerAccessToken) {
        id
        orders(first: 250, reverse: true) {
            nodes {
                ...OrderCoreFields
                lineItems(first: 100) {
                    nodes {
                        ...OrderLineItemFields
                    }
                }
            }
        }
    }
}