query GetProducts($first: Int!) {
    products(first: $first) {
        edges {
            cursor
            node {
                ...ProductCard
            }
        }
        pageInfo {
            hasNextPage
            hasPreviousPage
            startCursor
            endCursor
        }
    }
}
