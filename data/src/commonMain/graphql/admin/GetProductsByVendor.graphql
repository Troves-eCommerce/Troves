query GetProductsByVendor($first: Int!, $query: String!, $locale: String!) {
    products(first: $first, query: $query) {
        edges {
            node {
                ...ProductCard
            }
        }
        pageInfo {
            hasNextPage
            endCursor
        }
    }
}
