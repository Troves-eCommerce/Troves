query GetProductsByVendor($first: Int!, $query: String!) {
    products(first: $first, query: $query) {
        edges {
            node {
                ...ProductCard
            }
        }
        pageInfo {
            hasNextPage
            endCursor
        }
    }
}
